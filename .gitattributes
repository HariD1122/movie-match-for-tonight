# Keep line endings normalised in the repo regardless of who clones it.
* text=auto eol=lf

# Binary-ish files git should not touch.
*.png binary
*.jpg binary
*.ico binary
*.woff2 binary

# Collapse lockfile noise in diffs and reviews.
package-lock.json linguist-generated=true -diff
